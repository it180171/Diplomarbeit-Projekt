import CircularProgress from '@mui/material/CircularProgress';

const LoadingProgress = () => {
    return (
        <div className="loadingContainer"><CircularProgress className="loading" /></div>)
}

export default LoadingProgress